// Este archivo puede ser reemplazado durante la compilación usando el fileReplacements array.
// `ng build --configuration production` reemplazará `environment.ts` con `environment.prod.ts`.
// La lista de reemplazos de archivos se puede encontrar en angular.json.

export const environment = {
  production: false,
  apiUrl: 'https://localhost:44342/api'
};